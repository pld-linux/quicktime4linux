diff -uNr quicktime.orig/jpeg_old.h quicktime/jpeg_old.h
--- quicktime.orig/jpeg_old.h	Mon Oct  1 05:16:38 2001
+++ quicktime/jpeg_old.h	Fri Nov  8 19:02:43 2002
@@ -11,7 +11,7 @@
 #endif
 
 
-#include "jpeg/jpeglib.h"
+#include <jpeglib.h>
 #include <png.h>       /* Need setjmp.h included by png.h */
 #include "pthread.h"
 
diff -u quicktime/libdv.c~ quicktime/libdv.c
--- quicktime/libdv.c~	Fri Dec  7 17:20:32 2001
+++ quicktime/libdv.c	Tue Feb  4 03:43:14 2003
@@ -417,12 +417,12 @@
 	{
 		pthread_mutexattr_t attr;
 		dv_initted = 1;
-		dv_init();
+		dv_init(0,0);
 		pthread_mutexattr_init(&attr);
 		pthread_mutex_init(&dv_lock, &attr);
 	}
 
-	dv->decoder = dv_decoder_new();
+	dv->decoder = dv_decoder_new(0,0,0);
 	dv->decoder->quality = dv->decoder->video->quality;
 	dv->decoder->prev_frame_decoded = 0;
 	dv->use_mmx = 1;
@@ -575,7 +575,7 @@
 	if(!dv->temp_audio[0])
 	{
 		for(i = 0; i < 4; i++)
-			dv->temp_audio[i] = calloc(1, sizeof(gint16) * DV_AUDIO_MAX_SAMPLES);
+			dv->temp_audio[i] = calloc(1, sizeof(dv_coeff_t) * DV_AUDIO_MAX_SAMPLES);
 	}
 
 	switch(size)
@@ -684,7 +684,7 @@
 			videoseg.k = v;
 			videoseg.isPAL = isPAL;
 
-			dv_process_videosegment(input_rows, 
+			process_videosegment(input_rows, 
 					     &videoseg, 
 						 target + offset,
 					     3,
@@ -696,7 +696,7 @@
 	
 	
 	
-	write_meta_data(target, 0, isPAL, &now);
+	_dv_write_meta_data(target, 0, isPAL, &now);
 	
 }
 
diff -uNr quicktime.orig/libdv.h quicktime/libdv.h
--- quicktime.orig/libdv.h	Mon Dec  3 14:48:32 2001
+++ quicktime/libdv.h	Fri Nov  8 19:02:43 2002
@@ -17,12 +17,12 @@
 #define u_int64_t unsigned long long
 #endif
 
-#include "libdv/dv.h"
+#include <libdv/dv.h>
 #include <pthread.h>
 #include <sys/time.h>
 
 #ifdef HAVE_FIREWIRE
-#include "libraw1394/src/raw1394.h"
+#include <libraw1394/raw1394.h>
 
 typedef struct
 {
diff -uNr quicktime.orig/libmjpeg.h quicktime/libmjpeg.h
--- quicktime.orig/libmjpeg.h	Wed Nov 28 16:03:55 2001
+++ quicktime/libmjpeg.h	Fri Nov  8 19:02:43 2002
@@ -30,7 +30,7 @@
 
 
 #include <stdio.h>
-#include "jpeg/jpeglib.h"
+#include <jpeglib.h>
 #include <png.h>       /* Need setjmp.h included by png.h */
 #include "pthread.h"
 
